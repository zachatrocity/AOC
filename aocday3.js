//var input = document.getElementsByTagName("pre")[0].innerText;

var input =  "^>v<";

function node(count) {
	this.visitCount = count;
}

//santa visits first house
var block = [[new node(1)]];


void calcHouses = function(dir){
	switch(expression) {
	    case '^':
	        block.unshift()
	        break;
	    case '>':
	        //code block
	        break;
        case 'v':
	        //code block
	        break;
        case '<':
	        //code block
	        break;
	    default:
	        default code block
	}
}

var housesVisited = 0;

for (var i = 0; i < input.length; i++) {
	calcHouse(input[i]);
};



